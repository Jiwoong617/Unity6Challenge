public enum EnemyState
{
    Move,
    Skill,
    Die,
}

public enum ItemType
{
    Hp,
    Jump,
    Speed,
    Parry,
    Dodge,
}

public enum SceneType
{
    StartScene = 0,
    GameScene = 1,
    EndScene = 2,
}